describe("Tooltip", () => {
  it("Renders a tooltip", () => {
    // expect(queryByTestId('tooltip-1'))
  });
  it("Shows on mouseEnter and hides on mouseLeave", () => {
    // expect(queryByTestId('foo-1))
  });
  it("Hides in less than 300 milliseconds when mouseentering another tooltip", () => {
    // hover first
    // first is showing
    jest.advanceTimersByTime(100);
    // hover second
    // first was hidden
    jest.advanceTimersByTime(100);
    // second shows
  });
});
